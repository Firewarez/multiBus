openapi: 3.0.0
info:
  title: API MultiBus
  description: API de gerenciamento de rotas e localização de transporte público
  version: 1.0.0
  contact:
    name: MultiBus Team
servers:
  - url: https://multibus-api.onrender.com/api-docs
    description: Servidor de desenvolvimento

paths:
  /:
    get:
      summary: Endpoint de teste da API
      description: Retorna uma mensagem confirmando que a API está rodando
      responses:
        '200':
          description: API funcionando corretamente
          content:
            text/plain:
              schema:
                type: string
                example: "🚍 API MultiBus rodando!"

  /api/v1/stops:
    get:
      summary: Lista todas as paradas
      description: Retorna uma lista de todas as paradas de ônibus cadastradas
      tags:
        - Paradas
      responses:
        '200':
          description: Lista de paradas
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Stop'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      summary: Cria uma nova parada
      description: Cadastra uma nova parada de ônibus
      tags:
        - Paradas
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StopInput'
      responses:
        '201':
          description: Parada criada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Stop'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/stops/{id}:
    get:
      summary: Busca uma parada por ID
      description: Retorna os dados de uma parada específica
      tags:
        - Paradas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da parada
          schema:
            type: integer
      responses:
        '200':
          description: Dados da parada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Stop'
        '404':
          description: Parada não encontrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      summary: Atualiza uma parada
      description: Atualiza os dados de uma parada existente
      tags:
        - Paradas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da parada
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StopInput'
      responses:
        '200':
          description: Parada atualizada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Stop'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      summary: Remove uma parada
      description: Remove uma parada do sistema
      tags:
        - Paradas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da parada
          schema:
            type: integer
      responses:
        '200':
          description: Parada removida com sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Parada removida com sucesso."
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/lines:
    get:
      summary: Lista todas as linhas
      description: Retorna uma lista de todas as linhas de ônibus cadastradas
      tags:
        - Linhas
      responses:
        '200':
          description: Lista de linhas
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Line'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      summary: Cria uma nova linha
      description: Cadastra uma nova linha de ônibus
      tags:
        - Linhas
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LineInput'
      responses:
        '201':
          description: Linha criada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Line'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/lines/{id}:
    get:
      summary: Busca uma linha por ID
      description: Retorna os dados de uma linha específica
      tags:
        - Linhas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da linha
          schema:
            type: integer
      responses:
        '200':
          description: Dados da linha
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Line'
        '404':
          description: Linha não encontrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      summary: Atualiza uma linha
      description: Atualiza os dados de uma linha existente
      tags:
        - Linhas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da linha
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LineInput'
      responses:
        '200':
          description: Linha atualizada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Line'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      summary: Remove uma linha
      description: Remove uma linha do sistema
      tags:
        - Linhas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da linha
          schema:
            type: integer
      responses:
        '200':
          description: Linha removida com sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Linha removida com sucesso."
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/routes:
    get:
      summary: Lista todas as rotas
      description: Retorna uma lista de todas as rotas com informações das linhas
      tags:
        - Rotas
      responses:
        '200':
          description: Lista de rotas
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/RouteWithLine'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      summary: Cria uma nova rota
      description: Cadastra uma nova rota para uma linha
      tags:
        - Rotas
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RouteInput'
      responses:
        '201':
          description: Rota criada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Route'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/routes/{id}:
    get:
      summary: Busca uma rota por ID
      description: Retorna os dados de uma rota específica
      tags:
        - Rotas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da rota
          schema:
            type: integer
      responses:
        '200':
          description: Dados da rota
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteWithLine'
        '404':
          description: Rota não encontrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      summary: Atualiza uma rota
      description: Atualiza os dados de uma rota existente
      tags:
        - Rotas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da rota
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RouteInput'
      responses:
        '200':
          description: Rota atualizada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Route'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      summary: Remove uma rota
      description: Remove uma rota do sistema
      tags:
        - Rotas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da rota
          schema:
            type: integer
      responses:
        '200':
          description: Rota removida com sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Rota removida com sucesso."
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/routes/line/{lineId}:
    get:
      summary: Busca rotas por linha
      description: Retorna todas as rotas de uma linha específica
      tags:
        - Rotas
      parameters:
        - name: lineId
          in: path
          required: true
          description: ID da linha
          schema:
            type: integer
      responses:
        '200':
          description: Rotas da linha
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Route'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/routes/{id}/stops:
    get:
      summary: Lista paradas de uma rota
      description: Retorna todas as paradas de uma rota em ordem
      tags:
        - Rotas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da rota
          schema:
            type: integer
      responses:
        '200':
          description: Paradas da rota
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/StopWithOrder'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      summary: Adiciona parada à rota
      description: Adiciona uma parada existente a uma rota
      tags:
        - Rotas
      parameters:
        - name: id
          in: path
          required: true
          description: ID da rota
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RouteStopInput'
      responses:
        '201':
          description: Parada adicionada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteStop'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/routes/{routeId}/stops/{stopId}:
    delete:
      summary: Remove parada da rota
      description: Remove uma parada específica de uma rota
      tags:
        - Rotas
      parameters:
        - name: routeId
          in: path
          required: true
          description: ID da rota
          schema:
            type: integer
        - name: stopId
          in: path
          required: true
          description: ID da parada
          schema:
            type: integer
      responses:
        '200':
          description: Parada removida da rota com sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Parada removida da rota com sucesso."
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/notifications:
    get:
      summary: Lista todas as notificações
      description: Retorna uma lista de todas as notificações do sistema
      tags:
        - Notificações
      parameters:
        - name: is_active
          in: query
          required: false
          description: Filtrar por notificações ativas ou inativas
          schema:
            type: boolean
        - name: type
          in: query
          required: false
          description: Filtrar por tipo de notificação
          schema:
            type: string
            enum: [aviso, informacao, atraso, emergencia, manutencao]
        - name: priority
          in: query
          required: false
          description: Filtrar por prioridade
          schema:
            type: string
            enum: [baixa, media, alta, urgente]
      responses:
        '200':
          description: Lista de notificações
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Notification'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      summary: Cria uma nova notificação
      description: Cadastra uma nova notificação no sistema
      tags:
        - Notificações
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationInput'
      responses:
        '201':
          description: Notificação criada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
        '400':
          description: Dados inválidos
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/notifications/active:
    get:
      summary: Lista notificações ativas
      description: Retorna apenas as notificações que estão ativas no momento
      tags:
        - Notificações
      responses:
        '200':
          description: Lista de notificações ativas
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Notification'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/notifications/{id}:
    get:
      summary: Busca uma notificação por ID
      description: Retorna os dados de uma notificação específica
      tags:
        - Notificações
      parameters:
        - name: id
          in: path
          required: true
          description: ID da notificação
          schema:
            type: integer
      responses:
        '200':
          description: Dados da notificação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
        '404':
          description: Notificação não encontrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      summary: Atualiza uma notificação
      description: Atualiza os dados de uma notificação existente
      tags:
        - Notificações
      parameters:
        - name: id
          in: path
          required: true
          description: ID da notificação
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationUpdateInput'
      responses:
        '200':
          description: Notificação atualizada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
        '404':
          description: Notificação não encontrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      summary: Remove uma notificação
      description: Remove uma notificação do sistema
      tags:
        - Notificações
      parameters:
        - name: id
          in: path
          required: true
          description: ID da notificação
          schema:
            type: integer
      responses:
        '200':
          description: Notificação removida com sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Notificação removida com sucesso"
        '404':
          description: Notificação não encontrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/notifications/{id}/toggle:
    patch:
      summary: Ativa/Desativa uma notificação
      description: Alterna o status de ativo/inativo de uma notificação
      tags:
        - Notificações
      parameters:
        - name: id
          in: path
          required: true
          description: ID da notificação
          schema:
            type: integer
      responses:
        '200':
          description: Status da notificação alterado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
        '404':
          description: Notificação não encontrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/users:
    get:
      summary: Lista todos os usuários
      description: Retorna uma lista de todos os usuários cadastrados
      tags:
        - Usuários
      responses:
        '200':
          description: Lista de usuários
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      summary: Registra um novo usuário
      description: Cria uma nova conta de usuário no sistema
      tags:
        - Usuários
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRegisterInput'
      responses:
        '201':
          description: Usuário registrado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Dados inválidos
          content:
            application/json:
              schema:
                type: object
                properties:
                  errors:
                    type: array
                    items:
                      type: object
        '409':
          description: E-mail já cadastrado
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: "E-mail já cadastrado"
                  message:
                    type: string
                    example: "Este e-mail já está em uso. Tente fazer login ou use outro e-mail."
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/users/{id}:
    get:
      summary: Busca usuário por ID
      description: Retorna os dados de um usuário específico
      tags:
        - Usuários
      parameters:
        - name: id
          in: path
          required: true
          description: ID do usuário
          schema:
            type: integer
      responses:
        '200':
          description: Dados do usuário
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '404':
          description: Usuário não encontrado
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: "Usuário não encontrado"
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      summary: Atualiza dados do usuário
      description: Atualiza as informações de um usuário existente
      tags:
        - Usuários
      parameters:
        - name: id
          in: path
          required: true
          description: ID do usuário
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserUpdateInput'
      responses:
        '200':
          description: Usuário atualizado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Dados inválidos
          content:
            application/json:
              schema:
                type: object
                properties:
                  errors:
                    type: array
                    items:
                      type: object
        '500':
          description: Erro interno do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

components:
  schemas:
    Stop:
      type: object
      properties:
        id:
          type: integer
          description: ID único da parada
          example: 1
        name:
          type: string
          description: Nome da parada
          example: "Terminal Central"
        latitude:
          type: number
          format: double
          description: Latitude da parada
          example: -23.5505
        longitude:
          type: number
          format: double
          description: Longitude da parada
          example: -46.6333
      required:
        - id
        - name
        - latitude
        - longitude

    StopInput:
      type: object
      properties:
        name:
          type: string
          description: Nome da parada
          example: "Terminal Central"
        latitude:
          type: number
          format: double
          description: Latitude da parada
          example: -23.5505
        longitude:
          type: number
          format: double
          description: Longitude da parada
          example: -46.6333
      required:
        - name
        - latitude
        - longitude

    Line:
      type: object
      properties:
        id:
          type: integer
          description: ID único da linha
          example: 1
        code:
          type: string
          description: Código da linha
          example: "001"
        name:
          type: string
          description: Nome da linha
          example: "Centro - Terminal"
      required:
        - id
        - code
        - name

    LineInput:
      type: object
      properties:
        code:
          type: string
          description: Código da linha
          example: "001"
        name:
          type: string
          description: Nome da linha
          example: "Centro - Terminal"
      required:
        - code
        - name

    Route:
      type: object
      properties:
        id:
          type: integer
          description: ID único da rota
          example: 1
        line_id:
          type: integer
          description: ID da linha
          example: 1
        direction:
          type: string
          description: Direção da rota
          enum: [ida, volta]
          example: "ida"
      required:
        - id
        - line_id
        - direction

    RouteWithLine:
      type: object
      properties:
        id:
          type: integer
          description: ID único da rota
          example: 1
        line_id:
          type: integer
          description: ID da linha
          example: 1
        direction:
          type: string
          description: Direção da rota
          enum: [ida, volta]
          example: "ida"
        line_code:
          type: string
          description: Código da linha
          example: "1300"
        line_name:
          type: string
          description: Nome da linha
          example: "Terminal - Tambaú"
      required:
        - id
        - line_id
        - direction
        - line_code
        - line_name

    RouteInput:
      type: object
      properties:
        line_id:
          type: integer
          description: ID da linha
          example: 1
        direction:
          type: string
          description: Direção da rota
          enum: [ida, volta]
          example: "ida"
      required:
        - line_id
        - direction

    StopWithOrder:
      type: object
      properties:
        id:
          type: integer
          description: ID único da parada
          example: 1
        name:
          type: string
          description: Nome da parada
          example: "Terminal de Integração"
        latitude:
          type: number
          format: double
          description: Latitude da parada
          example: -7.1178567
        longitude:
          type: number
          format: double
          description: Longitude da parada
          example: -34.8900741
        stop_order:
          type: integer
          description: Ordem da parada na rota
          example: 1
      required:
        - id
        - name
        - latitude
        - longitude
        - stop_order

    RouteStop:
      type: object
      properties:
        id:
          type: integer
          description: ID único do registro
          example: 1
        route_id:
          type: integer
          description: ID da rota
          example: 1
        stop_id:
          type: integer
          description: ID da parada
          example: 1
        stop_order:
          type: integer
          description: Ordem da parada na rota
          example: 1
      required:
        - id
        - route_id
        - stop_id
        - stop_order

    RouteStopInput:
      type: object
      properties:
        stop_id:
          type: integer
          description: ID da parada a ser adicionada
          example: 1
        stop_order:
          type: integer
          description: Ordem da parada na rota
          example: 1
      required:
        - stop_id
        - stop_order

    Error:
      type: object
      properties:
        error:
          type: string
          description: Mensagem de erro
          example: "Erro interno do servidor"
      required:
        - error

    User:
      type: object
      properties:
        id:
          type: integer
          description: ID único do usuário
          example: 1
        nome:
          type: string
          description: Nome completo do usuário
          example: "João Silva"
        email:
          type: string
          format: email
          description: Email do usuário
          example: "joao@email.com"
        cpf:
          type: string
          description: CPF do usuário
          example: "12345678900"
        telefone:
          type: string
          description: Telefone do usuário
          example: "83987654321"
        nascimento:
          type: string
          format: date
          description: Data de nascimento
          example: "1990-01-01"
      required:
        - id
        - nome
        - email
        - cpf
        - nascimento

    UserRegisterInput:
      type: object
      properties:
        nome:
          type: string
          minLength: 3
          description: Nome completo do usuário
          example: "João Silva"
        email:
          type: string
          format: email
          description: Email do usuário
          example: "joao@email.com"
        cpf:
          type: string
          minLength: 11
          maxLength: 14
          description: CPF do usuário
          example: "12345678900"
        telefone:
          type: string
          minLength: 10
          maxLength: 15
          description: Telefone do usuário (opcional)
          example: "83987654321"
        nascimento:
          type: string
          format: date
          description: Data de nascimento
          example: "1990-01-01"
        senha:
          type: string
          minLength: 6
          description: Senha do usuário
          example: "senha123"
      required:
        - nome
        - email
        - cpf
        - nascimento
        - senha

    UserUpdateInput:
      type: object
      properties:
        nome:
          type: string
          description: Nome completo do usuário
          example: "João Silva"
        email:
          type: string
          format: email
          description: Email do usuário
          example: "joao@email.com"
        cpf:
          type: string
          description: CPF do usuário
          example: "12345678900"
        telefone:
          type: string
          description: Telefone do usuário
          example: "83987654321"
        nascimento:
          type: string
          format: date
          description: Data de nascimento
          example: "1990-01-01"
        senha:
          type: string
          minLength: 6
          description: Nova senha do usuário
          example: "novasenha123"

    Notification:
      type: object
      properties:
        id:
          type: integer
          description: ID único da notificação
          example: 1
        type:
          type: string
          description: Tipo da notificação
          enum: [aviso, informacao, atraso, emergencia, manutencao]
          example: "atraso"
        title:
          type: string
          description: Título da notificação
          maxLength: 200
          example: "Atraso na Linha 120"
        description:
          type: string
          description: Descrição detalhada da notificação
          example: "A linha 120 está com atraso de aproximadamente 15 minutos devido ao trânsito intenso na Av. Principal."
        created_at:
          type: string
          format: date-time
          description: Data e hora de criação
          example: "2025-11-24T10:30:00Z"
        updated_at:
          type: string
          format: date-time
          description: Data e hora da última atualização
          example: "2025-11-24T10:30:00Z"
        is_active:
          type: boolean
          description: Indica se a notificação está ativa
          example: true
        priority:
          type: string
          description: Prioridade da notificação
          enum: [baixa, media, alta, urgente]
          example: "alta"
        start_date:
          type: string
          format: date-time
          description: Data e hora de início da validade
          nullable: true
          example: "2025-11-24T08:00:00Z"
        end_date:
          type: string
          format: date-time
          description: Data e hora de término da validade
          nullable: true
          example: "2025-11-24T18:00:00Z"
        affected_lines:
          type: string
          description: Códigos das linhas afetadas (separados por vírgula)
          nullable: true
          example: "120, 175, 201"
        affected_stops:
          type: string
          description: IDs das paradas afetadas (separados por vírgula)
          nullable: true
          example: "1, 5, 12"
      required:
        - id
        - type
        - title
        - created_at
        - is_active
        - priority

    NotificationInput:
      type: object
      properties:
        type:
          type: string
          description: Tipo da notificação
          enum: [aviso, informacao, atraso, emergencia, manutencao]
          example: "atraso"
        title:
          type: string
          description: Título da notificação
          maxLength: 200
          example: "Atraso na Linha 120"
        description:
          type: string
          description: Descrição detalhada da notificação
          example: "A linha 120 está com atraso de aproximadamente 15 minutos devido ao trânsito intenso na Av. Principal."
        is_active:
          type: boolean
          description: Indica se a notificação está ativa
          default: true
          example: true
        priority:
          type: string
          description: Prioridade da notificação
          enum: [baixa, media, alta, urgente]
          default: "media"
          example: "alta"
        start_date:
          type: string
          format: date-time
          description: Data e hora de início da validade
          example: "2025-11-24T08:00:00Z"
        end_date:
          type: string
          format: date-time
          description: Data e hora de término da validade
          example: "2025-11-24T18:00:00Z"
        affected_lines:
          type: string
          description: Códigos das linhas afetadas (separados por vírgula)
          example: "120, 175, 201"
        affected_stops:
          type: string
          description: IDs das paradas afetadas (separados por vírgula)
          example: "1, 5, 12"
      required:
        - type
        - title

    NotificationUpdateInput:
      type: object
      properties:
        type:
          type: string
          description: Tipo da notificação
          enum: [aviso, informacao, atraso, emergencia, manutencao]
          example: "atraso"
        title:
          type: string
          description: Título da notificação
          maxLength: 200
          example: "Atraso na Linha 120"
        description:
          type: string
          description: Descrição detalhada da notificação
          example: "A linha 120 está com atraso de aproximadamente 15 minutos."
        is_active:
          type: boolean
          description: Indica se a notificação está ativa
          example: true
        priority:
          type: string
          description: Prioridade da notificação
          enum: [baixa, media, alta, urgente]
          example: "alta"
        start_date:
          type: string
          format: date-time
          description: Data e hora de início da validade
          example: "2025-11-24T08:00:00Z"
        end_date:
          type: string
          format: date-time
          description: Data e hora de término da validade
          example: "2025-11-24T18:00:00Z"
        affected_lines:
          type: string
          description: Códigos das linhas afetadas (separados por vírgula)
          example: "120, 175, 201"
        affected_stops:
          type: string
          description: IDs das paradas afetadas (separados por vírgula)
          example: "1, 5, 12"